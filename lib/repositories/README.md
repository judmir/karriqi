# Repositories

Put Supabase queries and mutations here (one file per aggregate or module boundary), e.g. `shopping-repository.ts`.

Keep components and route handlers thin: they call repositories or small application services, not raw table names.
