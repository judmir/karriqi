/** Temporary global access while the customizer is being reviewed. */
export const THEME_CUSTOMIZER_EMAIL = "judikarriei@gmail.com";

export function canUseThemeCustomizerEmail(
  email: string | null | undefined,
): boolean {
  void email;
  return true;
}
