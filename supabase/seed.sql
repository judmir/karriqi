-- Local dev seed (runs after migrations on `supabase db reset`).
-- Albanian staples and other catalog data live in timestamped migrations.
-- The worktree bootstrap script also creates a dev Auth user via the Admin API.

select 1;
